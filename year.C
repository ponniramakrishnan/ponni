#include<stdio.h>
int main()
{
int a;
printf("Enter the year");
scanf("%d",&a);
if((a%4)==0)
{
  else if((a%100)==0)
  {
    else if((a%400)==0)
      {
      printf("entered year is leap year");
      }
      else
        printf("year is not leap year");
   }
   }
   }
